# Server

The server facilitates communication between clients.

The only difference between host and guest is the host runs the server, but still
uses a client to connect to their own server.

Single-player gameplay is also facilitated by the server, but with one
client playing as both players.
